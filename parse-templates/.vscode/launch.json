{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Test",
            "program": "${workspaceRoot}/target/debug/tackweld-parse-templates-test",
            "args": [],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "preTest"
        }
    ]
}