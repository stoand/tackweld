{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Test",
            "program": "${workspaceRoot}/../target/debug/tackweld-test",
            "args": ["--nocapture"],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "preTest",
            "internalConsoleOptions": "openOnSessionStart"
        }
    ]
}